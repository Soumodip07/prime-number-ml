# Results

This folder contains evaluation results for different models:

- MLP (binary only)
- MLP (binary + modular)
- MLP (modular only)
- CNN (binary)
- Transformer (binary)

Each folder contains:
- Loss curves
- Probability distributions
- Background rejection / ROC curves

Comparison plots are available in the `comparison/` folder.